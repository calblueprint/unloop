/*
 * CaseNoteEntry Styles
 *
 * This contains all the styles for the CaseNoteEntry component.
 */

export const styles = (/* theme */) => ({
  headerBorder: {
    borderBottom: '2px solid #C4CACD',
    paddingBottom: 8,
  },
  justifyActions: {
    justifyContent: 'right',
  },
});

export default styles;
