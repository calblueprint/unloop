/*
 * CaseNoteForm Styles
 *
 * This contains all the styles for the CaseNoteForm component.
 */

export const styles = (/* theme */) => ({
  root: {
    backgroundColor: '#000000',
  },
});

export default styles;
